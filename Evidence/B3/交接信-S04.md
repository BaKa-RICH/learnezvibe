# 交接信：S04 正式采样

> 写于 2026-08-20 · 上一条战线 S03 已 PASS · 收信人：下一个 session 的我们

---

亲爱的下个 session：

我们从 2026-08-19 开始打的一仗，在 2026-08-20 打赢了：**S03 smoke 全链路 PASS**。这封信把"打完了什么、接下来要做什么、哪些决定已经板上钉钉"一次讲清楚，省得你再翻十几个文档、再踩一遍我们已经踩过的坑。

## 一、我们刚打完的一仗（S03 收尾）

1. **bug 根因已定案**：S03 两次失败不是 agent 的问题，是 OMAC loop 的读回 bug——content 交付走 `complete-unsealed` 免封印分支时，没人把 verification 附件 hydrate 给证据门，导致合法提交被误判 `verification is required`。修复在 oh-my-multica `206f3b4`（一处补 `_hydrate_worker_collect_evidence` + 回归测试），全量 101 失败基线零新增。
2. **真实环境验证通过**：write（WEEK-13 重试）和 review 双双过门收敛，无新增 gate 失败评论。
3. **全新 smoke PASS**（05:33-06:06，33 分钟，零人工干预）：collect → write → review 三节点**一次过门**，跨节点数据真实流动（write 周报覆盖 collect 四要点，review 出真评审结论），collect agent 在现行协议下提交合规。完整证据：`Evidence/B3/smoke/s03-final-pass.md`。

**S03 正式关闭。** 别去读 `session-handoff-20260819.md` 和 `archive/` 里的旧失败文档找根因——那是历史记录，含已被证伪的猜测，以 `s03-rootcause-fix-20260820.md` 为准。

## 二、你的任务：S04 正式采样（4 个样本）

按 `protocol.md`（已修订，见下）跑 `Native-01 / OMAC-01 / Native-02 / OMAC-02`，存 `Evidence/B3/samples/<id>/` + `sample-index.json`。指标定义、rubric A1-A5、失败/排除/成功率口径都在 protocol.md，**以协议原文为准，不临时改**。

### 开跑前必须解决的两件事

**① fixture 注入链路（已于 2026-08-20 解出，无需再设计）**：
- **权威副本**：`Evidence/B3/fixtures-v1/`（4 个冻结文件，哈希与 frozen-input.json 全一致，2026-08-20 从 WEEK-10 附件恢复，之前交接信文件曾丢失、项目总纲被误改——现已归位，禁止编辑）。
- **注入机制（已读源码确认）**：`manifest.meta.source_issues: [{issue_id: <根issue-uuid>, label: "B3 冻结 fixture"}]` → `_develop_source_refs`(loop.py:3960) 把根 issue 注入每个节点的 `work show` 上下文 → collect agent 用只读查询读根 issue 的 4 个附件并核对 SHA-256。**无需改 OMAC 代码**。
- **S04 执行步骤**：① 每样本 `multica issue create` 根 issue（共同任务正文 + 从 fixtures-v1 上传 4 个附件），记 UUID+attachment ID；② 样本 manifest 的 meta 加 source_issues 指向该根 issue；③ **必须把 collect 节点的 `contract.objective` 补上 fixture 读取要求**（smoke 的 objective 没写，协议 §35/§54 已要求 collect 读根 issue fixture，manifest 属控制信息可补）；④ 照常 `dag run/tick`。
- **collect objective 模板句（直接复制进样本 manifest 的 collect 节点）**：
  ```text
  收集本周数据: 本周数据仅指冻结输入 b3-weekly-fixture-v1。四个冻结文件已作为附件附在根 issue 上(见上游 source_issues): 交接信-新session.md、项目总纲-Multica工作流引擎.md、Evidence/B1卡点清单.md、Evidence/B2验收记录.md。请从根 issue 附件下载这 4 个文件、核对 SHA-256 与 frozen-input.json 一致后, 汇总成 3-5 条要点写入 weekly-data.md; 不得使用仓库/workdir 中的同名文件, 不得使用动态网页。
- 注意：`交接信-新session.md` 是冻结 fixture，哈希锁在 `frozen-input.json`，**绝不能改**；活版 `项目总纲` 与冻结版字节不同，实验用冻结版（fixtures-v1）。

**② 执行顺序——已定案：配对并发**。`Native-01`+`OMAC-01` 同窗、`Native-02`+`OMAC-02` 同窗（同一环境窗口配对比较）；禁止同一 arm 并发、禁止四样本全并发。protocol.md 已于 2026-08-20 修订并重新冻结。理由：配对并发的环境一致性比串行更强，且省一半墙钟；残余风险（限流诱发额外 run）在本机容量下可接受。**第一对跑的时候盯一眼是否限流重试，干净就继续。**

## 三、已定案的决策（不再重议，除非你有强理由并升级用户）

- **WEEK-15 reviewer 合同质量意见**（gate 命令弱于验收声明）：**S04 前不采纳**，维持现口径。理由：现口径已被 S03 全链路验证；一致性查验由 A4 rubric 人工查验兜底。未来若采纳，**一次性按固定计划**：收紧三节点 gate 命令 → `dag check --no-review` lint → 重新 smoke → 修订 protocol.md 重新冻结 → 两 arm 同步。**不要反复尝试。**
- **B4：现在不写 plan**。B4 是 B3 的父阶段（将引入 reviewer reject/rework/再评审/收口），等 B3 完成（S04 数据 + S05 报告）后再规划。
- **不降级 PR 模式**，content 模式保留。
- **执行顺序**：见上②。

## 四、指标与读数注意事项（都是踩过的坑）

- **collect 可能跑 2 个 run**（smoke 出现过：第一个没提交、第二个交）。usage 按协议去重汇总没问题，但**run 数是独立指标**，别混。
- **证据门只验结构不验语义**：早上的 review 原样重发 write 的文件也过了门。S04 的 A4（review 检查一致性）要**人工查验 run-message**，不能只信过门。
- token/耗时/哈希三条线已验证可采集（`multica issue usage` / `issue runs` / metadata refs）。**run-message 拉取（`multica issue run-messages`）还没实际拉过一次，开跑前先用 smoke 的 WEEK-16/17/18 试拉确认格式。**
- 全量测试基线 = 101 个 Windows 环境失败（快照 `oh-my-multica/tests_baseline_101_a605a34.txt`）；定向 7 文件 ~40s。**两套口径（9 vs 101）别混。**

## 五、纪律提醒（血泪换的）

- **没读过源码的命令，先读源码再跑**；涉及平台/agent 的命令一律带 timeout 或后台。
- `dag check` = lint + agent 评审流程，且评审轮询有 bug（无限挂死，见排障手册问题 24）；`dag run` = 无 sleep 连轴循环；**后台驱动用 `dag tick` + sleep**（模板 `.omac/smoke-driver.sh`）。
- 配对并发也逐样本后台跑，用户零值守。

## 六、文件导航

- **协议（修订后）**: `Evidence/B3/protocol.md`
- **计划账本**: `plan/B3计划.md`（S03✅ S04 就绪）
- **3 分钟速查**: `Evidence/B3/next-session-entry.md`
- **S03 最终证据**: `Evidence/B3/smoke/s03-final-pass.md`
- **根因与修复**: `Evidence/B3/s03-rootcause-fix-20260820.md`
- **排障手册（新增问题 24）**: `omac联调排障手册.md`

祝顺利。这轮跑完，B3 就有第一批真实数字进简历了。

—— 2026-08-20 的我们
