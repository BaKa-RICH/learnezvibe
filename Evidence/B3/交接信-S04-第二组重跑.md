# 交接信：S04 第二组重跑（污染修复 + 缓解后重跑）

> 写于 2026-08-20 19:45（本地）· 收信人：跑"按缓解选项重跑第二组"的 session
> 前置必读：`Evidence/B3/samples/incident-pair-02-contamination.md`（第二组污染事件完整记录，本信是它的执行化）

---

## 一、当前状态（一句话）

**第一组双 SUCCESS（Native-01 + OMAC-01，有效）；第二组（Native-02 + OMAC-02）双样本被污染，已归档证据并标记 PENDING_USER_DECISION；现在按"清场 + 同窗串行"缓解方案重跑第二组。**
- 第一组证据：`Evidence/B3/samples/Native-01/`、`OMAC-01/`、`pair-01-complete.md`
- 第二组污染记录：`Evidence/B3/samples/incident-pair-02-contamination.md` + `Native-02/`、`OMAC-02/`（原始证据 + summary.json）

## 二、第二组为什么污染（两条通道，重跑必须堵死）

### 通道 1：跨 arm 实时串扰（Native-02 停滞）
3 个 weekly agent（collect/write/review）**两臂共用**。OMAC-02 collect（11:11:28 起）与 Native-02 collect 成员（11:12:49 起）**并发派发同一 agent**，成员 run 的会话上下文带着 OMAC run id，直接读 OMAC run-messages 后判定"collect 已由受控 DAG 任务 WEEK-26 执行"，拒绝产出；leader 信以为真记 no_action，管线永久停滞。
**堵法：两臂同角色派发必须不重叠**——见第三节执行配方（同窗串行）。

### 通道 2：跨样本仓库残留（OMAC-02 write/review 照抄第一组）
第一组证据 `Evidence/B3/samples/Native-01/deliverables/{weekly-data,weekly-report}.md` **已 commit 并推送到 GitHub 远端 main**（commit 0700d2f 等）。OMAC-02 write/review agent 克隆 learnzvibe 仓库后 `rg --files` 搜到并读取，write 产物与 Native-01 报告仅差行尾空格，review 交付物与 Native-01 **字节完全一致**（sha 003dc99582e3 相同）。
**堵法：重跑前把 `Evidence/B3/samples/` 整个移出 repo 并推送远端**（详见第四节清场）。

### 为什么第一组没踩到
- 通道 1：第一组两臂 collect **不重叠**（Native 09:00-09:03，OMAC 09:20 起）——纯属时序幸运。
- 通道 2：第一组跑的时候 `samples/Native-01/deliverables/` 还不存在（证据是跑完才采集入库的）。

## 三、执行配方（重跑，同窗串行）

> **需用户先确认**：把"配对并发"修订为"配对串行（同窗内先跑完一臂再跑另一臂）"。这是小修订，发生在重跑样本开始前，符合"样本未开始可修订"原则；且这是唯一能根治通道 1 的方式（3 个 weekly agent 两臂共用是冻结配置，不改 agent 就只能在派发时序上错开）。

**先跑 Native-02-rerun，完全收敛后再跑 OMAC-02-rerun**（Native 快 ~15min，先验证配方）：

1. **清场**（见第四节，务必先做）
2. **Native-02-rerun**：
   - 建新根 issue：`multica issue create --title "[B3-S04 Native-02] weekly 周报流程 (正式样本)" --description-file .omac/s04/root-issue-body.md` + 4 个 fixtures-v1 附件（路径 `Evidence/B3/fixtures-v1/...`）
   - `multica issue assign <id> --to "周报"`（squad 6e94e57b-9c9d-4380-9304-5b9d780112f1）
   - leader 自动派发 collect→write→review；监控至 **root issue 达 done/in_review 且 review 完成**（~15-20 min）
3. **等 Native 完全收敛，确认无污染告警**（第五节 canary 1），**再**建 OMAC-02-rerun
4. **OMAC-02-rerun**：
   - 建新根 issue（同正文+同附件）→ 复制 `.omac/s04-omac-01.yaml` 为 `.omac/s04-omac-03.yaml`（**不要复用 -02**）：改 title→OMAC-02、`meta.source_issues[0].issue_id`→新根 issue、三节点 `status: todo`/`work_item_id: null`；collect 的 `contract.objective` 模板句保留
   - `nohup bash .omac/s04-driver.sh .omac/s04-omac-03.yaml .omac/s04-omac-03.log >/dev/null 2>&1 &`
   - 等 CONVERGED（rc=0，~25 min）
5. **采集**（照 `sample-checklist.md`，模板在归档目录，见第四节）：root-issue.json / runs.json / runmsg-<run>.json / deliverables(下载+哈希) / summary.json / comment-thread.json(Native) / driver.log(OMAC)；逐样本 A1-A5 核对
6. **污染 canary 全绿**（第五节）才算有效样本；任一告警 → 停下升级用户

## 四、清场（重跑前必做，含远端）

```bash
cd /d/agentlearn/learnezvibe
# 0) 归档：整个 samples 目录移出 repo（含 tracked + untracked 的所有历史样本证据）
mkdir -p /d/agentlearn/learnezvibe-b3-archive-20260820
mv Evidence/B3/samples /d/agentlearn/learnezvibe-b3-archive-20260820/samples
#    归档后 sample-checklist.md 在 /d/agentlearn/learnezvibe-b3-archive-20260820/samples/sample-checklist.md
# 1) 提交删除并推送远端（远端 main 必须干净——agents 克隆的是它）
git add -A
git commit -m "chore(B3): 重跑前清场-移除历史样本证据(防 agent 复用)"
git push origin main
# 2) 验证远端干净：临时 clone 检查
git clone https://github.com/BaKa-RICH/learnezvibe.git /tmp/verify && ls /tmp/verify/Evidence/B3/samples   # 应报不存在
find /tmp/verify -name "weekly-*.md" | head   # 除 fixtures-v1 外不应有输出文件
rm -rf /tmp/verify
```

**保留不动**：`Evidence/B3/fixtures-v1/`（冻结输入，本来就该在）、`protocol.md`、`frozen-input.json`、`交接信*.md`、`.omac/s04/root-issue-body.md`、`.omac/s04-omac-01.yaml`、`s04-driver.sh`、3 个 weekly agent 的 OMAC env（OMAC_ENGINE/WORKSPACE_ID/PROJECT_ID，别删）。

**注意**：OMAC 引擎 run 期间会向 repo 自动提交 "chore(omac): manifest sync"（观测到 19:16-19:36 多次），属正常，不影响清场结果。

## 五、污染告警 canary（重跑后逐条核，全绿才算有效）

### canary 1（Native-02-rerun 完成后）
- [ ] collect 成员 run 的 run-messages 不含 `WEEK-2[5-9]`、`run-messages 01a0`、任何 OMAC 工单引用（grep runmsg 验证）
- [ ] weekly-data.md 由 Native 成员产出，hash 为新的（≠ 14caa249 / 5245b7f6 / c87bb483）
- [ ] root issue 终态 done/in_review，A1-A5 逐项可核

### canary 2（OMAC-02-rerun 完成后）
- [ ] write run-messages 不含 `Native-01`、`samples`、`deliverables` 路径引用；产物 hash ≠ 003dc99582e3 / 0f1e46f46364 / a623b42e / 14caa249
- [ ] review 的 verification yaml（含 review-consistency.py）env_setup/summary **不指向** Evidence/B3/samples
- [ ] 三节点 run 时间线：同 agent 无并发重叠（collect→write→review 串行推进）
- [ ] review 交付物 = write 交付物（透传）或内容自洽，且 hash 与历史样本全不同

## 六、踩坑清单（本 session 血泪换的）

1. **run-messages 格式确认可用**：`multica issue runs <issue-id>` 拿 run id → `multica issue run-messages <run-id>`（均 --output json）
2. **`issue get` 才有 attachment id**（create 响应不含 attachments 明细）
3. **OMAC 的 run 在 DAG 节点 issue 上**（WEEK-21/22/23、26/27/28 模式），根 issue 没有 run；监控脚本 `.omac/s04-monitor.sh` 从 manifest 读 work_item_id 逐节点拉（复用，改 root issue id）
4. **driver 一律 dag tick + sleep**，绝不 dag check（评审轮询挂死，排障手册问题 24）
5. **大批会话空转/截断 → 先查 codex 账号余额**（问题 25 同窗口事件：欠费表现像并发问题）；首个 run 几分钟内应有 usage，无 usage 立即停
6. **旧 WEEK-24（污染 Native-02）仍指派在周报 squad 上，勿动勿复用**；重跑一律新根 issue；旧 manifest `s04-omac-02.yaml` 勿复用
7. 平台会去掉 description 尾部换行（正文逐字一致性按"去尾换行后相等"判定，正常）
8. 平台/agent 命令一律 timeout 或后台；没读过源码的命令先读源码
9. 附件下载命令（采集用）：`multica attachment download <id> -o <dir>`
10. fixtures 哈希（collect 自算核对用）：交接信 a3553b02 / 项目总纲 17c5cb0d / B1 40cb15e9 / B2 9d744387（frozen-input.json 权威）

## 七、需用户再确认的 3 点（新 session 开工前问一次）

1. **执行方式修订**：配对并发 → 同窗串行（先 Native 后 OMAC，同角色不重叠）。← 推荐，唯一根治通道 1
2. **槽位命名**：沿用 Native-02/OMAC-02（协议"排除补足"口径）并标注 rerun，还是开新槽 Native-03/OMAC-03？
3. **清场动作**：`Evidence/B3/samples/` 移出 repo + push 远端，确认执行

## 八、S05 说明（重跑完成后）

- 4 样本汇总（Native-01/02 + OMAC-01/02）：四类 token / 墙钟 / 步骤数 / run 数 / 成功率
- Native 两种口径：成员三节点 vs 含 leader 编排开销
- 报告两层：给用户的一页结论（项目语言）+ 硬核明细，存 `Evidence/B3/reports/`
- 第二组污染的两个样本单列披露（不计入计量，protocol 修订口径同"故障 run 不计入"）
