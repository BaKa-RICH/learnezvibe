# 交接信：S04 第二组重跑（清场 + 同窗串行，新槽 Native-03/OMAC-03）

> 写于 2026-08-20 19:55（本地）· 收信人：执行重跑的 session
> 前置必读：`Evidence/B3/samples/incident-pair-02-contamination.md`（污染事件原文，**已归档**，见第四节路径）

---

## 一、当前状态（一句话）

**第一组双 SUCCESS（Native-01 + OMAC-01，有效）；第二组（Native-02 + OMAC-02）双样本污染已排除并归档；用户已拍板：清场完成、同窗串行、新开槽 Native-03/OMAC-03 重跑。重跑完成后再做 S05 分析。**

**用户已拍板（2026-08-20，已登记 protocol 修订记录，新 session 无需再问）：**
1. 执行方式：配对并发 → **同窗串行**（先跑完一臂全部三节点，再启动另一臂）
2. 槽位：**新开 Native-03 / OMAC-03**（污染的第二组样本排除，不计入计量、单列披露）
3. 清场：**已执行**——`Evidence/B3/samples/` 已整体移出仓库并推送远端（commit d94df09）

## 二、第二组为什么污染（两条通道，重跑必须堵死）

### 通道 1：跨 arm 实时串扰（Native-02 停滞）
3 个 weekly agent（collect/write/review）**两臂共用**。OMAC-02 collect 与 Native-02 collect 成员**并发派发同一 agent**，成员 run 从会话上下文读到 OMAC run id，判定"collect 已由受控 DAG 任务执行"拒绝产出；leader 信以为真记 no_action，管线停滞。
**堵法**：同窗串行——两臂同角色派发不重叠（本配方已内置）。

### 通道 2：跨样本仓库残留（OMAC-02 write/review 照抄 Native-01）
第一组证据 `Evidence/B3/samples/Native-01/deliverables/` 已 commit 并推送到 **GitHub 远端 main**。OMAC-02 write/review agent 克隆仓库后 `rg --files` 搜到并读取，write 产物与 Native-01 报告仅差行尾空格，review 交付物字节完全一致。
**堵法**：清场（已做）+ **本次重跑的证据采集必须暂存仓库外**（见第三节第 5 步，防 Native-03 证据污染 OMAC-03）。

## 三、执行配方（重跑，同窗串行：先 Native-03 后 OMAC-03）

1. **（已完成）清场**：`Evidence/B3/samples/` → `/d/agentlearn/learnezvibe-b3-archive-20260820/samples/`，commit d94df09 已 push，远端已验证干净。
2. **Native-03**：
   - 建新根 issue：`multica issue create --title "[B3-S04 Native-03] weekly 周报流程 (正式样本)" --description-file .omac/s04/root-issue-body.md` + 4 个 fixtures-v1 附件（`Evidence/B3/fixtures-v1/` 下，路径在 cwd 内）
   - `multica issue assign <id> --to "周报"`（squad 6e94e57b-9c9d-4380-9304-5b9d780112f1）
   - leader 自动派发 collect→write→review；监控至**根 issue 达 done/in_review 且 review 完成**（~15-20 min）
3. **Native-03 完全收敛 + canary1 全绿后，才建 OMAC-03**（严禁两臂同角色并发）
4. **OMAC-03**：
   - 建新根 issue（同正文+同附件）→ 复制 `.omac/s04-omac-01.yaml` 为 `.omac/s04-omac-03.yaml`（**勿复用 -02**）：改 `meta.title`→OMAC-03、`meta.source_issues[0].issue_id`→新根 issue、三节点 `status: todo`/`work_item_id: null`；collect 的 `contract.objective` 模板句保留
   - `nohup bash .omac/s04-driver.sh .omac/s04-omac-03.yaml .omac/s04-omac-03.log >/dev/null 2>&1 &`
   - 等 CONVERGED（rc=0，~25 min）
5. **证据采集（重要：先暂存仓库外）**：
   - 两臂各自 run 期间，证据（runs.json / runmsg-*.json / deliverables / summary 等）**先写到 `/d/agentlearn/learnezvibe-b3-archive-20260820/tmp-collect/`（仓库外）**
   - **两臂都收敛 + canary 全绿后**，才把证据移入 `Evidence/B3/samples/Native-03/`、`OMAC-03/`（此时无后续 run，不会再生污染）
   - 逐样本照归档的 `sample-checklist.md`（`/d/agentlearn/learnezvibe-b3-archive-20260820/samples/sample-checklist.md`）核对 A1-A5

## 四、归档路径（新 session 从这里读模板和污染原文）

- 模板：`/d/agentlearn/learnezvibe-b3-archive-20260820/samples/sample-checklist.md`
- 污染原文：`/d/agentlearn/learnezvibe-b3-archive-20260820/samples/incident-pair-02-contamination.md`
- 第一组证据：`/d/agentlearn/learnezvibe-b3-archive-20260820/samples/Native-01/`、`OMAC-01/`、`pair-01-complete.md`
- 污染样本证据：`.../samples/Native-02/`、`OMAC-02/`（S05 单列披露用）

## 五、污染告警 canary（重跑后逐条核，全绿才算有效样本；任一告警 → 停下升级用户）

### canary 1（Native-03 完成后）
- [ ] collect 成员 run 的 run-messages 不含 `WEEK-2[5-9]`、`run-messages 01a0`、任何 OMAC 工单引用
- [ ] weekly-data.md 由 Native 成员产出，hash ≠ 历史（14caa249 / 5245b7f6 / c87bb483 / 003dc995 / 0f1e46f4）
- [ ] 根 issue 终态 done/in_review，A1-A5 逐项可核

### canary 2（OMAC-03 完成后）
- [ ] write run-messages 不含 `Native-01/02/03`、`samples`、`deliverables` 路径引用；产物 hash 全新
- [ ] review 的 verification yaml（含 review-consistency.py）env_setup/summary **不指向** samples/deliverables 路径
- [ ] 三节点 run 时间线：同 agent 无并发重叠（collect→write→review 串行推进）
- [ ] review 交付物 = write 交付物（透传）或内容自洽，hash 与历史样本全不同

## 六、踩坑清单（血泪换的）

1. **run-messages 格式**：`multica issue runs <issue-id>` 拿 run id → `multica issue run-messages <run-id>`（均 --output json）
2. **`issue get` 才有 attachment id**（create 响应不含）
3. **OMAC run 在 DAG 节点 issue 上**（根 issue 无 run）；监控脚本 `.omac/s04-monitor.sh` 从 manifest 读 work_item_id 逐节点拉（复用，改 root issue id）
4. **driver 一律 dag tick + sleep，绝不 dag check**（评审轮询挂死，排障手册问题 24）
5. **大批会话空转/截断 → 先查 codex 账号余额**；首个 run 几分钟内应见 usage，无 usage 立即停
6. **旧 WEEK-24/25（污染样本根 issue）勿动勿复用**；重跑一律新根 issue（WEEK-29+）；旧 manifest `s04-omac-02.yaml` 勿复用
7. 平台去掉 description 尾部换行属正常（正文一致性按去尾换行后相等判定）
8. 平台/agent 命令一律 timeout 或后台；没读过源码的命令先读源码
9. 附件下载（采集用）：`multica attachment download <id> -o <dir>`
10. fixtures 哈希（collect 自算核对用）：交接信 a3553b02 / 项目总纲 17c5cb0d / B1 40cb15e9 / B2 9d744387
11. OMAC 引擎 run 期间会向 repo 自动 commit "chore(omac): manifest sync"，属正常；重跑期间**不要**把任何样本证据 commit 进仓库

## 七、S05 说明（重跑完成后）

- 4 样本汇总（Native-01/03 + OMAC-01/03）：四类 token / 墙钟 / 步骤数 / run 数 / 成功率
- Native 两种口径：成员三节点 vs 含 leader 编排开销
- 报告两层：给用户的一页结论（项目语言）+ 硬核明细，存 `Evidence/B3/reports/`
- 第二组污染样本（Native-02/OMAC-02）单列披露，不计入计量（protocol 修订口径）
