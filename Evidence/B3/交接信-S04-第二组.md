# 交接信：S04 第二组（Native-02 + OMAC-02）+ S05 分析

> 写于 2026-08-20 · 第一组（Native-01 + OMAC-01）已双 SUCCESS · 收信人：跑第二组并做 S05 分析的 session

---

亲爱的下个 session：

S04 第一组已经在 2026-08-20 跑完，**Native-01 和 OMAC-01 双双 SUCCESS**（A1-A5 全达标）。你不需要从零摸索——第一组已经把路趟出来了，还顺带修好了两个环境坑。这封信告诉你：第一组怎么跑通的、你第二组照抄哪些步骤、要避哪些坑、以及跑完后 S05 分析要做什么。

## 一、当前状态（一句话）

**第一组完成（双 SUCCESS），第二组待开跑；第二组跑完 → 做 S05 对比分析 → B3 收口。**
完整证据：`Evidence/B3/samples/pair-01-complete.md`（硬核报告）+ `Evidence/B3/samples/Native-01/` 和 `OMAC-01/`（原始证据）。

## 二、第一组怎么跑通的（第二组照抄这套已验证步骤）

### 公共前置（已就绪，不用重做）
- **fixture 权威副本**：`Evidence/B3/fixtures-v1/`（4 个冻结文件，哈希与 frozen-input.json 全一致，禁止编辑）。
- **共同任务正文**：`.omac/s04/root-issue-body.md`（从 protocol 逐字提取，Native 和 OMAC 的根 issue 都用它，保持字节一致）。
- **3 个 weekly agent 已配好 OMAC 环境变量**（OMAC_ENGINE/WORKSPACE_ID/PROJECT_ID）——这是第一组修好的坑，见第四节，别删。

### Native-02 步骤（平台侧）
1. `multica issue create`：标题 `[B3-S04 Native-02] weekly 周报流程 (正式样本)`，`--description-file .omac/s04/root-issue-body.md`，`--attachment` 传 fixtures-v1 的 4 个文件（路径在当前目录内）。
2. 指派 `周报` squad：`multica issue assign <id> --to "周报"`。leader 会自己在根 issue 评论区 @mention 委派 collect→write→review（第一组就是这么跑的，不会开子 issue，正常现象）。
3. 记录根 issue UUID + 附件 ID，存 `Evidence/B3/samples/Native-02/root-issue.json`。

### OMAC-02 步骤（引擎侧）
1. `multica issue create` 根 issue：同样正文 + 同样 4 个附件。
2. 复制 `.omac/s04-omac-01.yaml` 为 `.omac/s04-omac-02.yaml`，改三处：
   - `meta.title` → OMAC-02
   - `meta.source_issues[0].issue_id` → OMAC-02 根 issue UUID
   - 三个节点 `status: todo`、`work_item_id: null`（清空）
   - **collect 节点的 `contract.objective` 保留第一组那份模板句**（已在文件里，别动，它负责让 collect 读根 issue 的 4 个 fixture）
3. 后台有界驱动：`nohup bash .omac/s04-driver.sh .omac/s04-omac-02.yaml .omac/s04-omac-02.log >/dev/null 2>&1 &`（driver 模板就是第一组用的，**不用 dag check、不用 dag run 前台**）。

### 配对并发的触发节奏
- 两个根 issue 建好后，先启动 OMAC driver，再指派 Native-02（差几十秒即可，同一窗口）。

## 三、采集与判定（照 protocol 第 5 节 + 第一组实操确认）

- 每样本存 `Evidence/B3/samples/<id>/`：root-issue.json / runs.json / usage.json / run-messages / deliverables（下载附件）/ summary.json。
- **run-messages 格式已验证可用**（第一组实际拉过）：`multica issue runs <issue-id>` 拿 run id → `multica issue run-messages <run-id>`。
- A4（review 一致性）：OMAC 有 `review-consistency.py` 独立核验（verification yaml 里能看到）；Native 的 review 是声明式"通过验收"——**要人工看一眼 review 的评论/run-message**，别只信过门。
- 失败按协议记失败，不重跑替换；口径争议先升级用户。

## 四、已定案决策（沿用 + 第一组新增）

**沿用（不再重议）：**
- 配对并发（Native-02+OMAC-02 同窗；禁止同 arm 并发、禁止四样本全并发）。
- WEEK-15 reviewer 合同质量意见：不采纳，维持现口径。
- B4 暂不写 plan（等 B3 完成）。
- 不降级 PR 模式，content 模式保留。

**第一组新增的口径决定（2026-08-20，用户拍板）：**
1. **codex 故障期 run 不计入计量**。第一组 08:20-08:48 窗口因 codex 账号余额耗尽，出现一批空会话/截断 run（表现是 usage 为空或仅 1-3 条消息）。这些是基础设施故障，**不是业务行为，不计入 token/墙钟/run 数**，在证据里单列披露即可。计量的墙钟/run 数以"成功 run"为准。
2. **frozen-input.json 不附到根 issue**。collect 自算 4 个附件的 SHA-256，与冻结版一致即视为合规（第一组 collect 自算哈希全部匹配，实测通过）。根 issue 保持"4 个文件"不动。
3. **agent 环境变量修复是基础设施接线，不影响实验**。给 weekly agents 设的 OMAC env 只是让它们能找到引擎配置，不改模型/thinking/runtime，不改变业务输入。

## 五、读数注意（第一组踩出来的）

- **OMAC 每节点是独立新工单**，agent 从零读任务、重新下载上一步产物，所以 token（尤其 cache_read）明显高于 Native；Native 是同一工单评论区接力，上下文连贯更省。这是设计使然，不是异常。
- **Native 有 leader 编排开销**（leader 的调度 run 计入样本，protocol 规定）。Native 的 token 有两种口径：成员三节点 vs 含 leader 全样本，S05 分析时都要算。
- **review 深度两 arm 不同**：OMAC 有脚本级核验，Native 是声明式。这本身是实验发现（确定性 vs 成本），不是 bug。
- 第二组跑的时候，**第一组跑完就顺手核验一下 run-messages 拉取和附件下载命令**（都在第一节），避免命令卡壳。

## 六、S05 分析要点（第二组跑完后）

1. 汇总 4 样本（Native-01/02 + OMAC-01/02）的 token（4 类）、墙钟、步骤数、run 数、成功率。
2. 两种 Native 口径都要算：成员三节点 vs 含 leader。
3. 对比结论写成 `Evidence/B3/reports/`（或按当时约定）下的 B3 分析报告，供用户面试/项目素材用——**报告要分两层：给领导看的一页结论（项目语言）+ 给 S05 后续用的硬核明细**。
4. 报告完 B3 即收口，可进总纲阶段 C 的前置抽象准备（见第八节）。

## 七、纪律（沿用）

- 没读过源码的命令先读源码再跑；平台/agent 命令一律 timeout 或后台。
- 后台驱动用 `dag tick` + sleep，**不用 dag check**（评审轮询有 bug，排障手册问题 24）。
- 遇到"所有会话突然空转/截断"——先怀疑账号余额/资源问题，别急着改架构（第一组差点误判成并发问题，实为欠费）。

## 八、文件导航

- **协议**：`Evidence/B3/protocol.md`（修订记录已加"故障 run 不计入"口径）
- **3 分钟速查**：`Evidence/B3/next-session-entry.md`
- **第一组硬核报告**：`Evidence/B3/samples/pair-01-complete.md`
- **第一组原始证据**：`Evidence/B3/samples/Native-01/`、`OMAC-01/`
- **驱动模板**：`.omac/s04-driver.sh`（第一组 OMAC 的 manifest 在 `.omac/s04-omac-01.yaml`，可复制）
- **排障手册**：`omac联调排障手册.md`（新增问题 25：agent 找不到引擎配置）

祝第二组顺利。跑完 S05 一出，B3 就有完整真实数据了。

—— 2026-08-20 的我们
