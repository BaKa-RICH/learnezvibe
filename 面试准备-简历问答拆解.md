# 面试准备:简历问答拆解手册

> 配套简历:确定性多 Agent 工作流引擎|面向开源 Agent 平台的流程编排系统
> 生成日期:2026-08-16
> 使用方法:面试前按"证据地图"过一遍,重点背 P0 高危题(见文末)

---

## 一、简历原文

**核心项目:确定性多 Agent 工作流引擎|面向开源 Agent 平台的流程编排系统**

- 独立设计并实现确定性工作流引擎:针对多 Agent 平台原生"LLM 临场编排、执行不可复现"的问题,设计流程定义/运行实例/节点状态三层模型,以静态定义驱动执行、评审、规划多类角色 agent 按序协作,将多阶段任务的步骤偏差归零;将交付形态抽象为节点级声明,与任务角色解耦,支持代码、文档等多类交付形态混合编排,一次定义、多次复用。
- 设计长链路任务运行时:采用幂等推进循环,流程状态三重持久化于流程文件、平台工单与 Git;支持任务中断处理、断点续跑、已完成节点复用,agent 单任务 10 分钟级长链路(建单->唤醒->执行->验收->PR 合并)全自动收敛;完成事件驱动版设计:状态机入库、任务完成事件直接驱动节点推进、与平台原生动态编排嵌套组合。
- 构建声明式机器验收与失败管控体系:节点验收方式由流程定义声明,分层组合确定性代码校验与有界 LLM 评审,验证命令退出码、验收项全覆盖、交付以远端事实为准,LLM 无法凭自述通过验收;节点失败自动隔离下游,配合有界返工预算与显式人工决策,杜绝无限重试与静默失败。
- 建立工作流回归评测体系:将平台 6 个官方自动化场景改造为多节点回归测试集,结合步骤序列断言与 LLM-as-a-Judge 评审角色,对引擎与 Prompt 改动执行回归;建立"原生 LLM 编排 vs 工作流引擎"基线,单次流程 Token 消耗降低约 __%、总耗时缩短约 __%。
- 沉淀 Agent Harness 与跨平台工程实践:为 codex/claude/pi 三种异构 agent 运行时构建统一沙箱执行环境与 Agent 可读约束(协议命令、环境规范、工具链),定位并修复 15 类跨系统联调故障,沉淀防复发排障手册,支撑 Windows 本地与云端环境稳定运行。

---

## 二、方法论说明(来自调研)

面试官追问在验证三件事:**真实性**(真做过吗)、**参与度**(你站在哪)、**判断力**(为什么这么选)。每条简历要点 = 一个"主张",必须配"证据"(指标/实验/系统改动/权衡)。每条至少备三类追问钩子:

| 钩子类型 | 典型问法 |
|---|---|
| 决策类 | 为什么选这个方案,不用那个? |
| 失败类 | 哪里没奏效?最难的问题? |
| 影响类 | 结果怎么衡量?数据哪来的? |
| 架构类 | 具体怎么实现?扩展性? |
| 评估/安全类(AI 项目) | 怎么防作弊/伪造?评测怎么控波动? |

**标注约定**(本文档使用):
- [已做] = 有真实运行证据,可直接讲
- [方案] = 已设计未实施,讲技术方案并主动说明"在设计/规划中"
- [待测] = 阶段 B 计划产物,数字未出

---

## 三、证据地图(主张 → 证据速查)

| 简历主张 | 证据 | 状态 |
|---|---|---|
| 三层模型/静态定义 | 引擎源码(manifest/定义管理层设计)、平台源码调研(原生无声明式模型) | 模型已设计+外置版已跑通;事件驱动版 [方案] |
| 步骤偏差归零 | 原生不可复现实例(pi 卡死 35min/codex 绕协议);引擎侧步骤由定义决定 | 定性 [已做];定量 [待测] |
| 幂等推进循环/三重持久化/断点续跑 | 全链路实跑(git 历史三次尝试、recovery 标记、done 节点复用) | [已做] |
| 10 分钟长链路收敛 | 真实运行记录(205 条消息、11 分钟、PR 合并、exit 0) | [已做] |
| 证据门/远端合并确认 | 引擎校验逻辑逐条(退出码/覆盖/一致性);验证附件留痕 | [已做] |
| 返工预算/失败隔离/显式决策 | 四档预算、exit 20、retry/accept/abandon 三分支 | [已做] |
| 6 场景回归测试集/LLM-Judge | 官方 6 模板原文在手;reviewer 评审角色引擎已内置 | 测试集 [方案];数字 [待测] |
| 三 runtime 沙箱/15 类故障 | 排障手册(15 问题:现象/根因/解法/防复发)+ Windows 兼容补丁 5 处 | [已做] |

---

## 四、问答拆解(按简历条目分组,共 37 问)

### 条目 1:确定性工作流引擎(架构与选型)

**Q1.1 为什么用静态定义,不让 LLM 动态规划流程?**[已做]
> 答:这是核心权衡。原生的"LLM 临场编排"下,同样输入多次执行步骤不一致--我实测过:一个 agent 在 Windows 上执行全盘遍历卡死 35 分钟;另一个亲眼看到协议文档仍绕开执行。动态规划还有持续 token 开销(每步都要 LLM 决策)。我的方案是"设计时与运行时分离":流程结构(步骤/角色/验收)静态定义,运行时零 LLM 决策,机器推进。LLM 只出现在两个地方:设计期(可选的规划 agent 辅助生成定义,生成后经人工确认固化)和执行期(每个节点内 agent 干活)。代价是灵活性:没定义过的分支处理不了。但目标场景(周报、分流等重复性流程)结构是已知的,牺牲灵活性换确定性值得。
> 补:这个权衡后来被业界印证:Anthropic 官方把 workflows/agents 归为同一光谱(agentic systems),按"步骤能否预先枚举"选层;最动态的 AutoGen 也补加了 Workflow 类,Cursor 底层用 Temporal 确定性引擎托管 agent loop--业界正向"确定性骨架+节点内自主"收敛。社区实测佐证(2026-08-18 核查):上游 Multica issue #1943 有用户实测官方 LLM leader 方案(Squad)比直调 subagents 费约 6 倍 token,"每步临场决策的 token 开销"不是理论推演,是社区量化过的事实。

**Q1.2 三层模型具体是什么?为什么需要三层?**[已做]
> 答:Definition(流程定义,静态"菜谱")/Run(运行实例)/NodeRun(节点状态)。
> 为什么分三层:①定义与运行分离,同一定义可多次实例化,改动定义不影响进行中的运行(版本快照,平台原有 autopilot 规则版本化的先例验证了这个模式);②Run 是审计单元(一次执行的全部状态和证据挂在一个实例下);③NodeRun 是状态机单元(待执行/进行中/完成/失败),推进和失败隔离都在这层。
> 若只有一层(单文件流程),就没法做"流程库管理"和并发运行隔离。

**Q1.3 "步骤偏差归零"怎么定义、怎么验证?**[定性已做/定量待测]
> 答:定义:同一流程定义多次运行,步骤序列(谁做什么、顺序、验收)完全一致。
> 原生对照:LLM 临场编排下步骤序列是概率性的。我的验证方式:回归测试集(6 个场景)每个跑 N 次,断言步骤序列一致 + 每步验收通过。**这套测试集在设计落地中,数字待测**--所以简历上"归零"是结构性论断(步骤由定义+引擎决定,不经过 LLM),不是实测统计,这点我会主动说清。

**Q1.4 和 LangGraph 这类编排框架的区别?**[已做(分析)]
> 答:抽象层不同。LangGraph 是进程内图执行库:节点是函数(通常是 LLM/工具调用,秒级),图引擎直接调用,状态靠 checkpointer 快照。
> 我的系统是"平台外委托编排器":节点是一张平台工单,由自治 agent 在沙箱执行十分钟级任务,引擎不进入 agent 内部,只通过平台接口指挥和观察。
> 状态哲学也相反:LangGraph 认为自己是事实源(存快照);我认为平台才是事实源(agent 的进度、产物、PR 都在平台上),所以恢复用"对账"(问平台真实状态同步回来)而不是快照回放--外部状态会漂移,快照会过期。
> 如果用 LangGraph 驱动 agent 平台,它只覆盖我最薄的推进层,守则分发/证据校验/返工预算/合并收口全要自建,而且长轮询和快照两处都逆着框架天性。
> 顺带一提:LangGraph 的 subgraph(确定性图里嵌完整 agent 循环)证明"骨架确定+节点自主"的嵌套是成熟模式--我的事件驱动版设计与之同构。

**Q1.5 要支持并行/分支怎么扩展?**[方案]
> 答:并行:节点的依赖字段本来就是列表,把两个节点的依赖都指向同一前置即并行(运行时已有并发上限参数);分支:在节点完成事件后加条件路由表(定义里声明"满足条件 X 走节点 A,否则 B"),判定用机器可执行的规则(字段值/命令退出码),不引入 LLM 判断,保持确定性。这两项在路线图里,当前版本只实现了顺序。

**Q1.6 为什么基于 agent 平台构建,而不直接调 Claude Agent SDK / 本地 runtime?**(几乎必问)[已做]
> 答:三层回答。1架构上可替换:我的引擎抽象了数据面(WorkItemStore)和执行面(AgentRuntime)两个接口,平台只是其中一种实现,旁边还有本地 mock 引擎--证明不是绑死平台。2直调 SDK 要自己补齐的东西很多:任务队列与断电恢复(agent 跑 10 分钟期间进程死了怎么办)、执行环境管理(worktree/环境注入/凭证--联调中 15 类故障大多由平台 daemon 兜着)、人类干预界面(失败转人工时,人在哪看进度、在哪点重试--平台 UI 是现成的)、跨 agent 行为审计(205 条执行记录是平台存档的)。3"适配重"的根源是外挂形态的税,不是平台无价值:一份 2300 行适配换来 21 种 runtime+执行层+UI;终局方案是引擎并入平台原生执行路径,这笔税直接消失--这正是我做事件驱动版设计的动机。

**Q1.7 你系统里 LLM 用在哪、代码用在哪?**[已做]
> 答:严格分离。LLM 只在三处:①设计期可选的规划流水线(planner/orchestrator 角色生成流程定义,经人确认固化);②每个节点内 agent 干活;③评审节点(reviewer 充当 LLM-as-a-Judge)。其余全是代码:结构推进、派发、证据门校验、失败隔离、合并收口。一个细节:连 planner/orchestrator 也是通过平台派活机制跑的普通 agent,引擎自身零 LLM 调用、零 SDK 依赖——确定性不依赖任何模型行为。

**Q1.8 多 agent 协作模式有好几种(群聊/父子/swarm/工作流),为什么选工作流?**[已做]
> 答:协作模式是一个光谱:群聊讨论(AutoGen GroupChat,灵活但 token 不可控)、supervisor 父子(Claude subagents,主上下文保护)、swarm handoff(OpenAI Agents SDK,控制权转移)、静态工作流(最可控)。我梳理过同进程协作的六种通信机制(共享状态/黑板/讨论/checkpoint/handoff/结果回传)各自的适用场景。我的目标场景(重复性业务流程)结构已知,选静态工作流拿确定性;但设计里预留了受控通道(验收不过时的 worker-reviewer 修复讨论、handoff 式受控动态边),不是全有或全无--确定性优先,按需开口子。

**Q1.9 平台原生明确不做声明式 workflow,你的改造是不是违背平台理念?**(必考,与 Q1.6 成镜像)[已做论证+方案]
> 答:三层论证。①平台能力中立:issue/评论/事件/调度对编排方式是中立的,而且平台自身已有确定性骨架(阶段闸门是代码判断不是 LLM),它只是在"节点验收"和"下一步决策"两个点把控制权交给 LLM--我是把这两个点变成可选声明,渐进类型之于 Python,不是对抗;②业界收敛:GitHub 双轨制(官方原话 "CI/CD needs to be deterministic")、AutoGen 后补 Workflow 类、CrewAI 同一套抽象一个参数切换两种编排、Gartner 判断市场收敛于"确定性流程在外、AI 有限自治在内"--补 workflow 是被验证的补全;③嵌套互补不是取代:leader 的两个职责被拆开,决策给状态机,验收按节点声明(代码门或 reviewer agent),leader 甚至可以作为一个节点留在流程里;真正风险是入口割裂(调研结论),所以设计上与原生编排嵌套组合,不做并列的两扇门。
> 补(2026-08-18 核查):社区需求本身就是证据--上游有四个 open 的相关 issue(#1245 并行/依赖管理、#1943 workflow 编排、#1998 轻量 handoff 原语、#4325 声明式工作流):#1245 用户跑 13-14 个 issue DAG,靠自建 "external watchdog polling issue status" 打补丁,呼吁原生 blocked_by;#1998 原话 "workflow's correctness lives inside each agent's prompt rather than in declarative platform config"。我的补全方向与社区诉求一致,且 100 个 fork 生态里零人实现过(源码级核查),不是重复造轮。

**Q1.10 引擎只支持代码交付?怎么通用化?**[已做骨架:B2 落地,mock 全链路验证]
> 新事实(2026-08-19,B2):节点级 delivery_mode(pr|content)已实现,单一策略模块路由全部差异点
> (lint pr_base/evidence pr_url/merge 收口/PR 封印/done 终态/评审因果基线/mock 交付);weekly 三节点
> 零 PR 字段过 check + mock 全链路收敛含评审 verdict=pass;PR 形态回归相对基线零新增。
> 剩余:真实引擎上的 content 全链路(B4)。
> 答:先定位卡点:引擎分流程结构/节点执行/失败处理/交付验收收口四层,前三层已通用,硬编码只在交付层--所以改造面被精确隔离,不是重写。方案:交付形态做成节点级声明(不是全局开关--混合流程里分析节点走内容交付、修复节点走 PR 交付),任务角色与交付形态解耦。工程方法:不用散点 if(散弹式修改反模式),收敛为单一策略入口;遵循"三次抽象律"--周报场景是第二样本,bug 分流当第三样本验证抽象边界;硬验收标准:新形态声明面零 PR 字段+现有流程测试回归全绿。
> 口径:简历写"多类交付形态"是留开放;当前具体设计了两类(代码 PR/内容文档),新增形态是加声明不是改引擎。

### 条目 2:长链路运行时

**Q2.1 幂等推进循环每步具体做什么?**[已做]
> 答:每轮循环四步:①状态对账(reconcile):从平台拉取所有进行中工单的真实状态,同步回本地流程文件;②结果回收(collect):对已完成的工单执行证据门验收;③失败隔离:发现失败节点,把依赖它的下游全部标记阻塞(防止下游白干);④调度派发(decide+dispatch):找"未开始且依赖已满足"的节点,建工单、写元数据、指派 agent。
> 幂等的关键:全部状态在流程文件+平台,循环本身无状态,任何时刻中断,重启后重新对账即续跑。

**Q2.2 为什么三重持久化?谁是 source of truth?**[已做]
> 答:平台工单是 source of truth(agent 的执行记录、验证附件、PR 状态只存在于平台和 GitHub);本地流程文件是工作镜像(引擎的记账本);git 是快照备份(每次状态变化自动提交,可审计可回溯)。
> 一致性策略:不做分布式事务,做对账--每轮循环以平台状态为准修正本地镜像。因为本地永远可以从平台重建,所以不怕本地丢失;平台数据有版本化附件和评论链,天然可追溯。

**Q2.3 断点续跑具体怎么工作?有真实例子吗?**[已做]
> 答:恢复流程:重启引擎 -> 加载流程文件(含每个节点状态和工单 ID)-> 对账拉平台真实状态 -> 已完成节点直接复用,进行中的继续观察,未开始的重新派发。
> 真实例子:我的一次运行,git 历史完整记录了三次尝试--第一次建单后被阻塞、第二次进行中被环境问题中断、第三次修复环境后续跑成功合并。流程文件和 git 快照让整个过程完全可复盘,这也是"状态进 git"设计的价值。

**Q2.4 为什么用轮询不用事件驱动?**[已做+方案]
> 答:外置 CLI 形态下拿不到平台内部事件钩子,只能轮询--这是形态约束不是设计偏好。
> 我做过对应的事件驱动版设计:引擎并入平台原生执行(Go),任务完成事件直接驱动推进(数据库状态机+完成事件处理链),从轮询变事件。设计已完成(含去重约束、事件总线多实例问题的分析),**未实施**。面试里我会主动讲这个对比,它说明我理解两种形态的取舍。

**Q2.5 10 分钟长链路里 agent 不听话怎么办?**[已做]
> 答:这是这个项目最核心的问题,我分三层防线:
> ①守则引导(prompt 层):工单内容强制规定"第一动作必须是读任务事实包,之前禁止任何其他操作",实测 agent 有时会先违反再自我纠正——引导有效但无保证;
> ②证据门强制(代码层):agent 交活必须提交结构化证据(每条验证命令+退出码+验收覆盖),缺一项打回,LLM 无法凭自述通过;
> ③返工预算兜底:打回后有限次重试,超限转人工决策。
> 结论:确定性来自机器验收,不来自 LLM 自觉。这句话是被实测教训出来的。

**Q2.6 agent 被唤醒时,任务上下文怎么组装?它怎么知道要读什么?**[已做]
> 答:三层信封+推拉结合。推给它的:①平台 daemon 拼的任务书(内联触发评论全文+交接说明+读数据的路标命令);②工作目录里的上下文文件(任务元数据+起步命令);③引擎 work show 命令返回的动态事实包(按节点×角色收窄:目标/验收标准/精确的提交命令)。它自己拉的:issue 全文、评论历史、代码——但读法有协议(先扫线程目录一行一条,再挑重要的线程展开最近 30 条),防上下文爆炸。关键设计:不存全量快照(历史可能几百行),只给"取件码"(指针+命令),agent 按需自取。

**Q2.7 agent 的记忆跨轮怎么延续?**[已做]
> 答:平台存 session_id,下次唤醒同一 agent 处理同一任务时 daemon 会带原会话续跑(resume),agent 接着上次的记忆干;如果续不上(会话丢失),自动注入"连续性通知",指示 agent 用任务数据(issue+评论)补课。"优先续命、续不上有补课协议"的双保险,我在真实运行里验证过这条链路。

### 条目 3:机器验收与失败管控

**Q3.1 证据门具体校验什么?**[已做]
> 答:六项结构化校验,全部代码强制:①PR 链接必填;②流程定义里的每条验证命令必须出现在提交证据中且退出码为 0;③每个验收条目必须有对应的业务测试覆盖;④环境说明必填(声明了集成门时);⑤PR 目标分支必须与定义一致;⑥覆盖率必须达标。
> 全部通过才推进节点状态,任何一项失败打回返工。
> 补:"确定性代码门前置+有界 LLM 评审后置"的分层后来发现正是业界共识:Anthropic 官方 grader 栈同样是先程序化 grader 再 LLM grader;Intercom 官方明确推荐关键判断用代码--"代码比自然语言评估更可靠、更快、无幻觉"。

**Q3.2 LLM 能伪造证据吗?信任边界在哪?**(安全类)[已做]
> 答:诚实地说,验证命令的退出码是 agent 在自己沙箱里执行并申报的,引擎做的是结构校验,不重新执行--这是已知信任边界。
> 三层兜底:①结构完整性(漏交、少验、字段不一致必被抓);②远端事实(产物必须真实存在:PR 必须真的在 GitHub 上、合并状态以 GitHub 远端为准,不信本地请求结果);③全程留痕(契约和验证证据作为附件挂在工单上,可人工审计)。
> 所以伪造的代价是:要么流程走不动,要么留痕被审计发现。真正要消灭伪造,需要引擎在隔离环境重跑验证命令--这在事件驱动版的路线图里(引擎侧复验)。

**Q3.3 返工预算怎么设计?**[已做]
> 答:四个环节独立计数:worker(执行返工)/ci(集成检查)/review(评审打回)/merge(合并失败),每档独立上限(默认 3 次)。任一环节超限,流程停在"需人工决策"状态,绝不自动继续。
> 人工决策三个出口:retry(重置节点重跑,可换 agent)/accept(人工接受已知风险放行)/abandon(放弃该节点,解锁非硬依赖的下游)。设计原则:机器处理可预期的失败,人处理不可预期的。

**Q3.4 为什么失败自动隔离?为什么不自动重试?**[已做]
> 答:隔离是为了防连锁浪费:上游失败,下游基于错误前置干活是纯浪费,所以自动把下游标阻塞。
> 不自动重试:①成本(每次重试是真金白银的 token + agent 时间);②自动重试会掩盖根因--同一个环境故障重试十次还是失败,但日志看起来"引擎很努力"。停下来把决定权给人,才是对失败负责。

**Q3.5 有 agent 绕过协议的真实案例吗?怎么发现的?**[已做,最佳真实性证据]
> 答:有,而且我是靠"行为记录仪"而不是 agent 自述发现的。一次运行的事后复盘:agent 的自述说"我遵循了协议",但拉取它的 205 条执行消息记录,发现它开局就先执行了协议明令禁止的平台查询操作,之后才自我纠正;另一次它完全绕开协议自己交付,被证据门打回。
> 从此确立诊断铁律:不信 agent 自述,看操作记录(执行消息 -> 工单时间线 -> daemon 日志)。这个方法论后来沉淀进排障手册。

**Q3.6 任务的状态和元数据怎么管理?存哪?验收标准会被修改吗?**[已做]
> 答:双镜像+静态契约。状态:引擎手里的流程账本(YAML,进 git,可审计可恢复)+ 平台工单上的 metadata 标签(30+ 键:节点标识/四类返工计数/交付引用),轮询对账时用工单 metadata 把平台状态映射回账本节点,两者互为备份。验收标准:静态契约附件(工单上的一个文件,三用——agent 的任务书原文/证据门的校验基准/人工审计对照)。合同签了不改:节点返工改的是验证结果,契约一个字不动;真要改走显式修订流程(生成新版本+评审门)。这是"确定性"的一部分:标准本身也必须是确定的。

### 条目 4:回归评测体系(部分未做,主动标注)

**Q4.1 回归测试集怎么构建?**[方案]
> 答:选平台 6 个官方自动化场景(每日新闻/周报/bug 分流/PR 评审/依赖审计/文档检查),每个拆成多节点流程定义。断言三层:①步骤序列断言(N 次运行步骤完全一致);②节点级断言(每个 verification_commands 通过);③产物 schema 断言(输出文件结构/关键字段)。引擎或 prompt 改动后全量跑一遍对比。
> 现状:6 个模板的原文分析和拆解方案已完成,**测试集实现和跑批数据是下一步**。

**Q4.2 LLM-as-a-Judge 怎么保证评审本身稳定?**[方案+已做组件]
> 答:评审角色(评审收敛门)是引擎内置能力,已存在[已做组件];把它用于回归评测是方案。稳定性手段:①结构化裁决 schema(不是自由文本,是枚举 verdict+必填理由);②评审轮次有上界(默认 3 轮,防评审-返工死循环);③裁决冲突时以规则断言为准(机器评测 > LLM 评审);④同版本评审跑多次取多数。

**Q4.3 基线对比怎么控制变量?**[待测]
> 答:设计:同场景、同 agent、同模型参数、同输入,原生 LLM 编排 vs 引擎编排各跑 N 次取均值。指标:总 token、总耗时、步骤数、失败率。token 从平台执行记录提取。
> 预期引擎占优的来源(可解释):①省掉 leader agent 每步的临场决策开销;②任务上下文按节点按需收窄(不是全量上下文);③失败重试有界(原生可能无限打转)。**数字未测,简历上的空位等这批数据。**

**Q4.4 评测波动怎么控制?**[待测]
> 答:①固定模型参数(温度/版本);②引擎路径本身确定性(流程不随机),波动只来自 agent 执行层;③失败版本回滚(定义和引擎都在 git 里,可直接回退对比);④多次运行取分布,不只报均值。

### 条目 5:Harness 与跨平台工程

**Q5.1 15 类故障里最难的几个?**(最佳真实性证据)[已做]
> 答:挑三个讲:
> ①agent 沙箱环境漂移:agent 运行环境是 daemon 启动时的快照,我装了新工具后 agent 死活找不到,排查半天才发现"改环境必须重启 daemon"。根因是进程环境继承,解法是把工具规范到统一 bin 目录+重启策略,沉淀成环境规范。
> ②Windows 编码体系:整个链路(配置读取/子进程捕获/文件写入)默认 GBK,中文配置直接崩,验证文件摘要校验还因换行符翻译不一致失败(CRLF/LF)。全链路显式 UTF-8+LF 修复。
> ③GitHub 认证在沙箱不可交互:agent 沙箱读不到 Windows 凭据管理器,push 失败。解法:token 内嵌 remote URL + git 配置重写,并给 agent 预置好认证的仓库 checkout。
> 每一条都有"现象/根因/解法/防复发"四段记录,这个手册本身就是项目资产。

**Q5.2 沙箱执行环境怎么设计?**[已做]
> 答:三条规范:①工具链:所有 agent 需要的工具(编排 CLI/gh/git)统一放平台约定的 bin 目录,保证任何 agent 任何会话都能找到;②命令规范:给 agent 的协议命令必须是 PowerShell 精确可执行的--曾因命令里带 bash 风格的环境变量前缀,agent 执行不了干脆绕开整个协议,这个教训直接改了引擎的命令生成;③认证预置:git/gh 认证在沙箱外配置好(非交互),agent 只管用。

**Q5.3 怎么让 agent 守协议?prompt 还是代码?**[已做]
> 答:三层 prompt 体系(工单守则/项目级路由/动态任务事实包)+ 代码强制(证据门/元数据对账/远端确认)。明确说:prompt 层是引导,实测 agent 会违反也会自我纠正,但无保证;确定性的最后一道墙永远是代码。所以我的设计原则是:prompt 负责让守规矩的 agent 效率高,代码负责让不守规矩的 agent 过不了关。

**Q5.4 三种 runtime 怎么选型?**[已做]
> 答:实测选型,不靠口碑。三个候选(claude/codex/pi)跑同一任务:codex 协议遵循最好、环境自救能力强(缺工具会自己装);claude 可用;pi 在 Windows 上执行了自杀式全盘遍历(卡死 35 分钟,人工取消)。结论:执行节点用 codex,并把这个选型过程记录成案例。启示:agent 的"能力口碑"不可信,必须在自己的目标环境实测。

### 通用深挖题

**G1 这个项目最大的技术挑战?**[已做]
> 答:不是单点技术,是"三系统 × 双不确定性":编排引擎、agent 平台、GitHub 三个系统的状态要一致(工程复杂度),同时 agent 行为不确定 + 环境不确定(运行时复杂度)。解法上,前者靠"平台为事实源的对账架构",后者靠"证据门+返工预算+显式人工决策"的分层防线。这两条主线贯穿了整个项目。

**G2 如果重来,会改什么设计?**[已做]
> 答:两点:①更早做事件驱动版,外置轮询版验证了模型但轮询形态天然低效;②一开始就奔"agent 无感"契约:现在 agent 要学协议(读事实包/结构化提交),对 agent 是额外负担,理想是让指令随平台原生的任务上下文下发,agent 零学习成本。这两点都是做出来之后才看清的代价。

**G3 怎么扩展到多实例/分布式?**[方案]
> 答:已知限制:当前 CLI 轮询形态 + 平台进程内事件总线,多实例会重复派发和事件丢失。方案(事件驱动版设计):状态机进数据库(行级状态+乐观锁)、事件走 DB 队列或广播、派发用分布式锁或单写者。平台的去重约束(同 issue+agent 只允许一个 pending 任务)给了天然幂等基础。

**G4 项目规模和数据?**(诚实回答)[已做]
> 答:个人研发项目:1 个工作空间、2 个活跃 agent(3 种 runtime 实测选型)、全链路真实运行记录(单任务 11 分钟/205 条执行消息/PR 合并)、15 类故障案例库、6 场景测试集方案。规模不大,但每一环都是真实跑通的,所有结论有留痕证据--这也是我能把每个追问答到细节的原因。

**G5 你研究过平台原生的多 agent 协作吗?和你的引擎什么关系?**[已做]
> 答:源码级调研过。原生协作是 supervisor-worker:队长(LLM)拿独家简报(协议+花名册+任务说明),在评论区@派活;worker 结果评论只回队长(worker 间无直连);上下文是"指针式注入"--不存快照,给取件码让 agent 自己拉;完成通知是事件驱动:阶段闸门(整阶段子任务全完成)关闭时平台发系统评论自动唤醒队长,单个完成静默不打扰。上游源码注释自己承认 "The server has no declarative workflow model - stages are agent-driven"(issue_child_done.go:444,2026-08-18 核验),拆解/选人/推进全部写在注入 leader 的 Squad Operating Protocol prompt 里(squad_briefing.go:20-79)。
> 我的引擎与它互补:原生消息丰富但零确定性,我的引擎确定性满格但零 agent 间消息。更准确地说是"拆解 leader":leader 身上的两个职责--验收(上个 agent 干完没)和决策(下一步派谁)--被 workflow 拆开,决策交给状态机,验收按节点声明可以是代码门也可以是 reviewer agent,leader 甚至可以作为一个节点留在流程里。事件驱动版设计直接复用原生事件链(把"唤醒队长"换成"推进状态机")--这也是事件驱动推进方案的接线点,大幅降低工程量。

**G6 agent 研发方向很多,你做的是哪种?**[已做]
> 答:我梳理过六大方向:垂直 agent 开发/编排平台 infra/agent 内核 runtime/评测可靠性/安全治理/协议生态。我的项目本体是编排平台 infra,同时天然向评测延伸(证据门=步骤级机器评测,轨迹记录已有)。往内核方向有低成本延伸路径(context 策略+记忆 provider 以插件形式下沉,2-4 周)。我不担心"只会编排"的标签:行业主流(如 OpenAI Codex agents 团队自己的定位)就是"模型外围的可信系统"--评测/验证/凭证据放行正是最稀缺的部分。

---

## 五、未完成项清单(诚实边界,面试主动说)

| 项 | 状态 | 面试话术 |
|---|---|---|
| 回归测试集实现+跑批 | 方案已设计,未实现 | "测试集设计和断言分层已定,实现是下一步" |
| Token/耗时基线数字 | 未测 | "简历数字位空着,测量方案已定(同变量 N 次取均值),不编数" |
| 事件驱动版 | 设计完成,未实施 | "外置版验证了模型,事件驱动版设计已完成,含事件/去重/多实例分析" |
| 并行/分支节点 | 未做 | "依赖模型天然支持并行,条件路由方案已有,当前版本只实现顺序" |
| 引擎侧复验(防伪造) | 未做 | "当前信任边界:结构校验+远端事实+留痕;引擎重跑验证在路线图" |
| 交付形态抽象落地 | 设计完成,mock/真实验证待做 | "四层分析定位卡点在交付层;策略骨架方案已定,周报场景第二样本、bug 分流第三样本验证抽象边界" |

## 六、P0 高危题(最可能被问 × 必须答好)

1. **Q1.6**(为什么基于平台不直调 SDK)--架构定位必考题,答不出平台价值=白做
2. **Q3.5**(agent 绕协议实例)--真实性核心证据,必考
3. **Q1.9**(改造是否违背平台理念)--改平台类岗位必问,答不好=没理解要改的对象
4. **Q1.1**(静态 vs LLM 动态的权衡)--架构判断力核心,现有业界印证加持
5. **Q3.2**(能不能伪造证据/信任边界)--安全钩子,答不好=没理解自己系统
6. **Q2.6**(上下文怎么组装/agent 怎么读数据)--AI 项目高频题,三层信封+推拉是标准答案
7. **Q4.3**(Token 降低怎么测的)--简历有数字空位,必被追问
8. **Q5.1**(最难故障)--工程能力最直接证明
9. **G5**(平台原生协作研究+"拆解 leader")--展示平台级理解的差异化解
10. **Q1.10**(怎么通用化)--高频架构钩子,Phase B 完成前以设计角度答
11. **G2**(重来改什么)--反思能力,忌答"没什么可改"

---

## 附:面试口述素材索引(不在简历,追问时展开)

- WEEK-8 全链路时间线(三次尝试/45 分钟排障/11 分钟执行/PR 合并)
- run-messages 行为记录仪方法(205 条消息复盘)
- LangGraph 深度对比(委托编排 vs 进程内图库;对账 vs 快照)
- 三层 prompt 体系细节(静态守则/动态事实/语义边界)
- 平台源码级调研(四类原生编排机制/事件总线/去重约束/数据库~100 张表结构)
- 平台原生协作消息流(supervisor-worker/评论白板/指针式注入/阶段闸门事件链)
- 引擎 vs 直调 SDK 七项对比(队列/环境/UI/审计/单点真相/一份适配换 21 runtime/云编本地执)
- 同进程协作六机制全景(共享状态/黑板/讨论/checkpoint/handoff/结果回传)
- agent 研发六方向地图(垂直开发/编排 infra/内核/评测/安全/协议生态)
- 业界对标弹药库:Anthropic 光谱论(agentic systems,按步骤可枚举性选层)/AutoGen 后补 Workflow 类/Cursor 底层用 Temporal 托管/GitHub 双轨制原话 "CI/CD needs to be deterministic"/Gartner 收敛判断/验收共识(Anthropic grader 栈先程序化再 LLM、Intercom "代码比自然语言评估更可靠")/嵌套三例(LangGraph subgraph/MAF WorkflowExecutor/CrewAI process 参数)/入口风险(模式并列割裂才是真风险)/"动态路径固化"叙事(先动态跑,重复路径固化为确定性子流程)
- 排障手册 15 问题全表(现象/根因/解法/防复发)
- B1 卡点实测的三次反转(pr_url 预判必卡->mock 塞假链接"假过";"mock 全链路跑不通"->根因是 merge 命令跟随 config engine 键、--engine 覆盖不传导->隔离 mocksite 后 3/3 收敛)--可讲 mock 测试可信度、配置覆盖不传导的排查方法论(以实测为准,不迷信预扫描)
- B2 落地过程:预判 3 个差异点,实测挖出 7 个(merge 收口外还有 PR 封印/done 终态 reconcile/评审因果基线/mock 假交付)--讲"抽象边界以实测为准"的三次抽象律实战;评审基线从封印时间换成 verification 附件时间的设计取舍
- B3-S03 hydration bug 复盘(2026-08-20,可讲 15 分钟): 表象"verification is required"→ 时间线证伪 agent 无责 → 真实数据逐环复现 → 架构根因=引用式附件的搬运责任缺位(content 免封印捷径没接上 PR 路径已有的 hydrate)→ TDD 红→绿 → 真实环境三节点类型全过门。讲: 分布式状态一致性 + 不要迷信第一直觉归咎 agent + 引用式设计的代价
- B3 实验设计(协议/样例已定,首批数字 S04 后回填): 冻结输入(fixture 哈希)+ 同拓扑 Native squad vs OMAC DAG 对比 + 4 类 token/wall_clock/步骤数/哈希链指标 + 失败样本处理口径 + 配对并发执行(同窗配对比较, 禁止同 arm 并发)

---

## 附二:项目完成后的全景(终态:Multica 长出流程引擎)

> 用途:面试被问"这个项目最终做成什么样/你的愿景"时的完整口述稿。原型阶段(外挂 CLI)只是验证模型,终态是引擎并入平台、外挂消失。

### 一句话

Multica 还是那个 Multica,但多了一种任务形态:以前 issue 只能交给 AI 临场发挥,现在 issue 可以挂上一条"标准流程"--平台自己按流程推进、验收、派发,不需要任何外挂工具。

### Multica 变成了什么样

**新增的东西:**

```
① 三张新表(数据库)
   workflow_definition  流程定义(菜谱:节点/顺序/验收标准)
   workflow_run         运行实例(这次执行的订单)
   node_run             节点状态(每道工序干到哪了)
   + issue 上多一个关联字段:这个工单属于哪条流程

② 事件链上一个新分支(核心改动,极小)
   原来:子工单完成 ──► 唤醒 leader AI ──► AI 决定下一步
   现在:子工单完成 ──► 这个工单挂了流程吗?
        ├─ 没挂 ──► 原路不变,唤醒 leader AI(一字不动)
        └─ 挂了 ──► 跑验收门 ──► 过:推进状态机,派下一个节点
                                └─ 不过:打回重做,超限通知人

③ 界面上的新东西
   - 创建/编辑流程定义(表单,或让 AI 帮你生成、你确认)
   - 流程运行视图(哪个节点在跑/完成/失败,像看快递物流)
   - 失败时的决策按钮(重试/接受风险/放弃)
   - 定时触发(每周一 9 点自动跑周报流程)
```

**没变的东西(这是关键):**

- issue、评论、附件、agent 调度、daemon、沙箱:原样
- 不挂流程的普通 issue:照旧交给 leader AI 临场发挥,一条代码没动
- worker agent:无感--收到的还是平台原生任务,不知道自己身处流程(它不用学任何新协议)

**消失的东西:**

- 外挂 CLI 工具(原型期用它验证了模型,使命完成,逻辑已翻译成平台代码)
- 轮询(原型期引擎靠每 30 秒问一次平台"干完没";现在是任务完成事件直接触发,零空转)

### 运行起来的样子(周报流程)

```
周一 9:00  定时触发 ──► 平台自动建流程实例,派第一节点
           ├─ 平台建工单"收集本周数据" ──► 叫醒 agent ──► agent 干完交证据
           ├─ 验收门核证据(命令结果/产物检查)──► 过 ──► 自动派"撰写周报"
           ├─ 撰写节点交稿 ──► 验收(格式校验 + 可选 AI 评审员复核)──► 过
           └─ 周报发布 ──► 全流程结束,界面标绿,git 式留痕可回放
中途任何节点:
   失败 ──► 下游自动挂起(不白干)──► 重试有界 ──► 超限弹决策按钮等你点
断电/重启 ──► 状态在数据库,自动接着干,完成的节点不重跑
```

### 用户的体验(前后对比)

| | 以前(Multica 原生) | 完成后(Multica + 流程引擎) |
|---|---|---|
| 发周报任务 | 写给 leader AI,它临场拆活 | 选一个现成流程,点运行(或定时自动) |
| 跑的过程 | 每次步骤可能不同 | 每次一模一样,界面能看每道工序 |
| 花费 | leader 每步"想一想"都花钱 | 省掉临场决策开销(基线数字待实测填) |
| 出问题 | 可能打转/悄悄失败 | 有界重试+自动挂起下游+弹按钮叫人 |
| 灵活任务 | 照旧临场发挥 | 照旧临场发挥(两条路并存,还能嵌套:leader 把重复部分派给流程) |

### 四个优点

1. **确定**:菜谱写死,换 AI 不换流程
2. **可靠**:AI 说过不算,验收门说了算
3. **共生**:不是取代 AI 编排,是给它补一个"标准流程"选项,两者能互相调用
4. **改动极小**:平台原生路径一行不动,全部新能力长在一条事件分支上

---

## 附三:面试项目组合(方向备忘)

**主线:Multica 工作流引擎**
简历五条与本文档 37 问描述的项目本体。完整计划见《项目总纲-Multica工作流引擎.md》:阶段 B->C->D->E,终态是"平台长出流程引擎"。面试材料随主线里程碑持续升级(见总纲第四章联动表)。

**支线:GPT Researcher 作为流程节点**
- 做什么:用 adapter 把开源研究 agent GPT Researcher 接成一个 runtime,在主线上跑一条"深度研究周报"流程(研究->撰写->评审)。
- 为什么值得做:它产出的是文档(真实的内容交付形态场景)、内部自带规划(节点内自主的活样本)、是异构成品 agent(证明交付形态抽象不是自产自销,能容纳外部知名 agent);接入走平台现成的 runtime 设施,增量在适配与编排验证。
- 时机与前置:主线内容形态(B4)跑通后启动;开工前需再派调研(接入方式/最新版本形态/适配成本)。
