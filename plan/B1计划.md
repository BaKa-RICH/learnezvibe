# B1 计划:weekly 卡点诊断(mock 引擎实测)

> 状态:未开工(2026-08-17,与交接信同期;草案已就绪,待用户确认开工)
> 本文件管 B1 动作;撞墙现场记录在 `B1卡点清单.md`(执行时逐条追加,不预写结论)。

## 目标(来自总纲 3.1)

用 mock 引擎跑手写 weekly manifest,实测卡点。
Done 标准:卡点清单(每条带报错现场),对照总纲 2.5 迁移清单校验预判。

## 已就绪

- `.omac/weekly.yaml`:三节点 collect→write→review,content 形态(pr_base 留空),
  worker `${WEEKLY_WORKER:-alice}` / reviewer `${WEEKLY_REVIEWER:-bob}`(env 展开,mock/真引擎双环境复用)。
- YAML 语法与 contract 必填字段已校验通过(仅 pr_base 按设计留空)。

## 执行步骤

1. `omac dag check .omac/weekly.yaml --engine mock`
   → 预期:3 条 `contract.pr_base is required`(lint 对每个节点强制,含评审节点)
   → 记录现场(卡点①,硬卡点)。
2. 用测试副本 `weekly.test.yaml` 补 `pr_base: main` → 再 check
   → 预期通过 → 确认"留空必报错 / 填占位即放行",说明 lint 无 content 开关。
   (正式 weekly.yaml 保持 content 形态不被污染。)
3. `omac dag run weekly.test.yaml --engine mock`
   → 观察 evidence 门:mock 对 develop 节点自动生成假 pr_url + 能过门的
     verification(mock.py:402/507)→ 大概率"假过"
   → 记录:evidence 门被 mock 掩盖(卡点②,隐性;真引擎必卡)。
4. 观察 review 节点:mock 对评审生成 review_report 是否要求 PR 相关字段
   → 记录"content 形态评审节点"的行为(卡点③候选,为 B2 交付策略覆盖评审节点提供依据)。
5. 汇总:预判 vs 实测对照表 → 追加入 `B1卡点清单.md` → 对照总纲 2.5 迁移清单。

## 卡点预判(基于读码,待实测确认,以实测为准)

| # | 卡点 | 位置 | 实测预期 |
|---|---|---|---|
| A | lint 强制 `contract.pr_base` 必填(所有节点,无 content 开关) | lint.py | 必报 |
| B | evidence 门 `artifacts.pr_url` 必填 | evidence.py:209 | mock 假过,真引擎必卡 |
| C | worker 名必须在 agent pool(lint) | lint.py | 草案已用 env 展开规避 |

## 已定决策

- **保留三节点 + 评审人**(collect→write→review,bob 评审):对齐 B4 验收形态;
  B1 顺带验证 content 形态下评审节点行为,供 B2 交付策略模块覆盖评审场景。
- **worker 用 env 展开**(`${WEEKLY_WORKER:-alice}`):mock 默认 alice/bob(在 mock pool),
  真引擎设环境变量即可,manifest 文件不因环境改动。
- **pr_base 留空为正式形态**:content 交付本无 PR base;留空用于拿到卡点①报错现场。

## 纪律(交接信护身符)

- 禁改 `.omac/manifest.yaml`(WEEK-8 现场);禁对已有 issue(尤其 WEEK-8)任何写操作。
- 实验性修改只在 `weekly.test.yaml` 副本上做,正式 `weekly.yaml` 保持 content 形态。
- 卡点以实测为准,不迷信预扫描。

## 记录约定

- 报错现场:落盘 `B1卡点清单.md`(每条带原始报错文本,作为 B2 输入原料)。
- 对话汇报:只给易懂结论(撞了什么墙/为什么/是否真卡点),不贴原始细节。
